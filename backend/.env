# Copy to .env and adjust
APP_ENV=dev
SECRET_KEY=change_me_dev_long_random
SESSION_COOKIE_NAME=eco_dms_session
FRONTEND_ORIGIN=http://localhost:5173
MOBILE_ORIGIN=exp://127.0.0.1:8081

# Pinata Configuration (get JWT from https://app.pinata.cloud/keys)
PINATA_JWT=

# IPFS Configuration (leave empty for Pinata-only mode)
IPFS_API_URL=http://127.0.0.1:5001/api/v0
IPFS_GATEWAY_URL=https://gateway.pinata.cloud/ipfs/

# JWT Secret (CHANGE IN PRODUCTION!)
JWT_SECRET_KEY=dev-secret-key-change-in-production-12345
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=1440

# CORS Origins (JSON array format)
ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=Eco-DMS Decentralized

# Blockchain
CHAIN_ID=1