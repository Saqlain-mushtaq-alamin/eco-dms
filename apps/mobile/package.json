{
  "name": "mobile",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "expo start --clear --tunnel",
    "start:lan": "expo start --clear --host lan",
    "start:localhost": "expo start --clear --host localhost",
    "android": "expo start --android --clear --host localhost",
    "ios": "expo start --clear --tunnel",
    "web": "expo start --web --clear",
    "doctor": "npx expo-doctor"
  },
  "dependencies": {
    "expo": "~54.0.0",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-native": "0.81.5"
  },
  "devDependencies": {
    "@expo/ngrok": "^4.1.0",
    "@types/react": "~19.1.10",
    "metro-minify-terser": "^0.84.2",
    "typescript": "~5.9.2"
  },
  "private": true
}