# ML requirements - Install only when you need ML verification
# Prerequisites: Install requirements-base.txt first
# Installation: pip install -r requirements-base.txt && pip install -r requirements-ml.txt

# ML Processing
celery==5.3.4
redis==5.0.1  # For Celery broker

# Deep Learning
torch>=2.0.0  # CUDA support optional
torchvision>=0.15.0
pillow>=10.0.0

# Object Detection
ultralytics>=8.0.0  # YOLOv8

# Image-Text Alignment (CLIP)
# Note: CLIP has installation issues on some systems
# Use open_clip as alternative:
open-clip-torch>=2.20.0
# OR install CLIP from source (uncomment if you want original CLIP):
# git+https://github.com/openai/CLIP.git

# Image Classification  
efficientnet-pytorch>=0.7.1

# Cryptography for signing
boto3>=1.28.0  # AWS KMS support (optional)

# Additional ML utilities
numpy>=1.24.0
scikit-learn>=1.3.0
